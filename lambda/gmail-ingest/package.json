{
  "name": "gmail-ingest-lambda",
  "version": "1.0.0",
  "description": "Lambda function to ingest Gmail emails and save to S3",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "package": "zip -r function.zip index.js node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.645.0",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.157",
    "@types/node": "^20.16.0",
    "typescript": "^5.5.4"
  }
}
